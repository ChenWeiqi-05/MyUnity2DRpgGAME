# RPG游戏存档系统分析与实现

---

## 一、功能描述

本存档系统为Unity引擎开发的2D动作RPG游戏提供完整的游戏状态持久化功能，涵盖玩家进度保存与恢复的全部核心需求。系统在游戏运行时自动或手动触发存档操作，将游戏状态序列化为JSON文件存储至本地磁盘；在游戏启动时检测存档文件并反序列化恢复游戏状态，实现游戏进度的无缝接续。

### 1.1 玩家经济数据持久化

系统负责保存与恢复玩家的货币数量（金币）。当玩家在游戏中获得或消耗货币时，变更后的数值将在存档时写入数据文件，确保玩家财富不会因退出游戏而丢失。读取存档时，货币数据被恢复至PlayerManager，玩家可以继续使用之前积累的财富。

### 1.2 背包与装备数据持久化

系统完整保存玩家的物品栏（Inventory）、储物箱（Stash）以及当前穿戴的装备（Equipment）信息。物品以"物品ID—堆叠数量"的键值对形式存储，装备以物品ID列表形式存储。存档恢复时，系统根据物品ID在物品数据库中查找对应的物品数据对象（ScriptableObject），重建物品实例并恢复装备的属性加成效果。

玩家死亡时，系统会自动保存并生成掉落物——当前持有的部分货币将掉落在死亡位置，玩家复活后可返回拾取。

### 1.3 技能树解锁状态持久化

游戏包含七种技能（剑术、格挡、闪避、冲刺、水晶、黑洞、分身），每种技能拥有多个可解锁的子技能节点，构成完整的技能树系统。存档系统将每个技能节点的解锁状态以"技能名称—解锁布尔值"的键值对形式保存。读取存档时，各技能脚本根据保存的状态重新激活已解锁技能的功能效果（如连击、瞬移、分身攻击等）。

### 1.4 关卡进度与重生点持久化

游戏场景中分布着多个检查点（Checkpoint），每个检查点具有唯一标识符（GUID）和激活状态。存档系统保存所有检查点的激活状态以及距离玩家最近的已激活检查点ID。读取存档时，系统恢复检查点的激活状态，并将玩家传送至最近激活的检查点位置，实现死亡后的合理重生产生点恢复。

### 1.5 音频设置持久化

系统保存玩家的音量设置参数，包括主音量、音乐音量、音效音量等多个音频混合器（Audio Mixer）参数的滑块值。读取存档时自动恢复玩家偏好的音量配置，避免每次启动游戏后重新设置。

### 1.6 存档管理功能

系统提供完整的存档生命周期管理：新游戏（NewGame）会删除旧存档并创建空白游戏数据；继续游戏（ContinueGame）检测存档文件存在性并加载恢复；删除存档（DeleteSavedData）移除磁盘上的存档文件。主菜单界面根据存档是否存在动态显示或隐藏"继续游戏"按钮。

### 1.7 多时机自动与手动存档

存档操作在以下时机触发：
- **游戏退出时**：通过`OnApplicationQuit()`自动保存，确保退出不丢进度；
- **玩家死亡时**：在`Die()`方法中自动保存，记录死亡位置和掉落货币；
- **场景重启时**：在`RestartScene()`中保存后重新加载场景；
- **手动退出**：在设置面板中选择"保存并退出"或在主菜单中按退出按钮/F4键。

---

## 二、功能设计

### 2.1 整体架构设计

存档系统采用**中心化管理器—数据接口—数据模型**三层架构，遵循单一职责原则和依赖倒置原则。

```
┌──────────────────────────┐
│      SaveManager         │  ← 中心调度器（单例）
│   (MonoBehaviour)        │
└──────────┬───────────────┘
           │ 调用
    ┌──────▼──────┐
    │ ISaveManager │  ← 接口层（解耦）
    │  (interface) │
    └──────┬──────┘
           │ 实现
    ┌──────▼──────────────────┐
    │ 12个实现类              │  ← 各子系统（数据提供者）
    │ GameManager/PlayerMgr/  │
    │ Inventory/UI/Skills...  │
    └─────────────────────────┘
           │ 读写
    ┌──────▼──────┐
    │  GameData   │  ← 统一数据模型
    └──────┬──────┘
           │ 序列化/反序列化
    ┌──────▼──────┐
    │FileDataHandler│  ← 磁盘I/O层
    └──────────────┘
```

### 2.2 核心设计模式

**（1）单例模式（Singleton Pattern）**

`SaveManager`作为存档系统的核心控制器，采用单例模式确保全局仅存在一个存档管理器实例。通过`DontDestroyOnLoad`标记使其在场景切换时不被销毁，保证整个游戏生命周期内存档管理的一致性。其他子系统如`GameManager`、`PlayerManager`、`Inventory`同样采用单例模式，形成全局访问点网络。

```csharp
if (instance != null)
{
    Destroy(gameObject);
    return;
}
instance = this;
DontDestroyOnLoad(gameObject);
```

**（2）接口隔离模式（Interface Segregation）**

定义`ISaveManager`接口，声明`LoadData(GameData _data)`和`SaveData(ref GameData _data)`两个方法。任何需要参与存档的子系统只需实现该接口，无需关心其他子系统的数据构成。`SaveManager`通过接口统一调度，实现了存档管理器与具体业务逻辑的解耦。

**（3）策略模式思想**

每个实现`ISaveManager`的子系统自行决定如何读写自己的数据片段，本质上是将数据序列化策略下放到各组件。`SaveManager`不关心每个子系统保存什么数据、如何保存，仅负责在适当时机遍历所有`ISaveManager`实例并调用其接口方法。

**（4）运行时类型发现（Runtime Type Discovery）**

`SaveManager`通过`FindObjectsOfType<MonoBehaviour>()`扫描场景中所有MonoBehaviour，并过滤出实现了`ISaveManager`接口的对象。这种动态发现机制无需在Inspector中手动拖拽绑定，自动适应场景中对象的增减，降低了配置维护成本。

### 2.3 统一数据模型设计

`GameData`类作为数据契约（Data Contract），集中定义了所有需要持久化的数据字段。类标记`[System.Serializable]`特性以支持Unity的JsonUtility序列化。关键设计决策包括：

- **使用自定义可序列化字典**：由于Unity的JsonUtility不支持标准`Dictionary`的序列化，系统封装了`SerializableDictionary<TKey, TValue>`类，通过`ISerializationCallbackReceiver`接口在序列化前将字典转换为并行的键列表和值列表，反序列化后重建字典。该设计解决了Unity引擎内置序列化器的限制。
- **冗余字段设计**：`lostCurrencyX`和`lostCurrencyY`独立存储而非嵌入复合对象，降低了JSON结构嵌套深度，便于序列化和调试查看。
- **构造函数默认值初始化**：所有集合类型在构造函数中初始化为空集合，避免空引用异常。

### 2.4 数据流转设计

**存档流程（Save）**：
1. `SaveManager.SaveGame()`被触发（退出/死亡/手动）
2. 重新扫描场景中所有`ISaveManager`实现者（捕获动态创建的对象）
3. 遍历每个`ISaveManager`，调用其`SaveData(ref GameData _data)`方法，各子系统将自身状态写入统一的`GameData`对象
4. `FileDataHandler.Save()`将`GameData`序列化为格式化的JSON字符串，通过`FileStream`写入磁盘文件

**读档流程（Load）**：
1. `SaveManager.Start()`中自动调用`LoadGame()`
2. `FileDataHandler.Load()`从磁盘读取JSON文件，反序列化为`GameData`对象
3. 对`GameData`中所有子对象进行空值校验，缺失字段补充默认空集合
4. 遍历每个`ISaveManager`，调用其`LoadData(GameData _data)`方法，各子系统从`GameData`中提取并恢复自身状态
5. 部分子系统（如Inventory、GameManager）采用延迟加载策略，等待一帧后执行恢复逻辑，确保依赖的UI组件和场景对象已完成初始化

### 2.5 存档文件设计

- **存储路径**：`Application.persistentDataPath + "/data.alexdev"`，在Windows上对应`%userprofile%\AppData\LocalLow\<company>\<product>\data.alexdev`
- **存储格式**：UTF-8编码的格式化JSON文本（`JsonUtility.ToJson(_data, true)`启用pretty-print）
- **文件命名**：存档文件名通过`SaveManager`的Inspector字段`fileName`配置，默认为`"data.alexdev"`
- **加密预留**：`FileDataHandler`中预留了基于XOR的加密解密逻辑（密钥`"alexdev"`），当前处于注释状态，通过`encryptData`布尔字段控制是否启用

---

## 三、运行效果

### 3.1 主菜单存档检测

游戏启动后进入主菜单场景（MainMenu），`UI_MainMenu`脚本在`Start()`中调用`SaveManager.instance.HasSavedData()`检测磁盘上是否存在存档文件。若存在，界面显示"继续游戏"（Continue）按钮；若不存在，该按钮隐藏，仅显示"新游戏"按钮。这一机制为玩家提供了直观的进度续接入口。

点击"新游戏"时，系统调用`DeleteSavedData()`清除旧存档，随后加载主场景并创建全新的`GameData`实例，所有游戏状态从零开始。点击"继续游戏"时，直接加载主场景，`SaveManager`在启动时自动读取存档恢复游戏状态。

### 3.2 游戏状态恢复流程

进入主场景后，`SaveManager`的`Start()`方法首先扫描场景中所有`ISaveManager`实现者，随后调用`LoadGame()`执行存档恢复。各子系统的恢复顺序和效果如下：

1. **技能树恢复**：7个技能脚本各自从`_data.skillTree`中读取对应按钮名称的解锁状态，通过协程延迟一帧检查并应用解锁效果（如解锁新技能图标、激活技能冷却系统等）。

2. **物品栏恢复**：`Inventory.LoadData()`遍历`_data.inventory`中的物品ID，在物品数据库中匹配对应的`ItemData` ScriptableObject，构建`loadedItems`和`loadedEquipment`列表。随后在`AddStartingItems()`中重建物品实例并恢复装备的属性加成。若存档中存在物品数据，则跳过初始物品的添加；若存档为空（新游戏），则添加预设的`startingItems`。

3. **关卡进度恢复**：`GameManager.LoadData()`通过协程延迟0.1秒后执行三项恢复操作——
   - 遍历所有检查点，匹配存档中激活状态为`true`的检查点并调用`ActivateCheckpoint()`激活
   - 将玩家位置传送至最近激活的检查点坐标
   - 若存在掉落货币（`lostCurrencyAmount > 0`），在保存的坐标位置实例化`lostCurrencyPrefab`并设置货币数量

4. **音量设置恢复**：`UI.LoadData()`遍历`_data.volumeSettings`，匹配各`UI_VolumeSlider`的参数名称并调用`LoadSlider()`设置滑块值，同时更新对应的Audio Mixer参数。

5. **货币恢复**：`PlayerManager.LoadData()`直接将存档中的货币数值赋值给`currency`字段。

### 3.3 存档写入触发与效果

**玩家死亡存档**：当玩家生命值归零时，`PlayerStat.Die()`计算本局损失货币数量（总货币的一定比例），调用`PlayerItemDrop.GenerateDrop()`在玩家位置生成掉落物品，最后调用`SaveManager.instance.SaveGame()`保存当前状态（包括损失货币的位置和数量）。玩家复活后可从检查点出发，返回死亡位置拾取掉落的货币。

**场景重启存档**：当玩家触发场景重启时，`GameManager.RestartScene()`先调用`SaveManager.SaveGame()`保存当前所有状态，再通过`SceneManager.LoadScene()`重新加载当前场景。由于`SaveManager`标记为`DontDestroyOnLoad`，场景重载后`Start()`将自动调用`LoadGame()`恢复游戏状态，形成"保存→重载→恢复"的循环。

**退出存档**：当玩家在设置面板选择"保存并退出"或在主菜单按F4/退出按钮时，系统调用`SaveManager.SaveGame()`写入存档，随后通过反射调用Unity编辑器的`isPlaying = false`（编辑器中）或`Application.Quit()`（独立构建中）退出游戏。

### 3.4 存档文件格式示例

实际存储在磁盘上的JSON文件（`data.alexdev`）结构如下：

```json
{
  "currency": 1250,
  "skillTree": {
    "keys": ["swordUnlockButton", "dashUnlockButton", "parryUnlockButton"],
    "values": [true, true, false]
  },
  "inventory": {
    "keys": ["a1b2c3d4...", "e5f6g7h8..."],
    "values": [3, 1]
  },
  "equipmentId": ["a1b2c3d4..."],
  "checkpoints": {
    "keys": ["cp_forest_01", "cp_cave_02"],
    "values": [true, false]
  },
  "closestCheckpointId": "cp_forest_01",
  "lostCurrencyX": 45.3,
  "lostCurrencyY": 12.7,
  "lostCurrencyAmount": 0,
  "volumeSettings": {
    "keys": ["MasterVolume", "MusicVolume", "SFXVolume"],
    "values": [0.8, 0.6, 1.0]
  }
}
```

---

## 四、实现分析

### 4.1 核心类实现

#### 4.1.1 SaveManager——存档调度中心

`SaveManager`是存档系统的神经中枢，负责协调所有子系统的数据存取。其核心实现要点包括：

**单例与生命周期管理**：在`Awake()`中实现单例判重和`DontDestroyOnLoad`标记，确保跨场景持久。`dataHandler`在`Awake()`中提前初始化，避免其他`Start()`调用者在`dataHandler`未就绪时访问。

**动态接口发现**：`FindAllSaveManagers()`方法使用`FindObjectsOfType<MonoBehaviour>()`扫描当前场景中所有继承自MonoBehaviour的对象，通过`is ISaveManager`模式匹配过滤出实现了接口的对象。该方法在`Start()`和`SaveGame()`中各调用一次，后者重新扫描以捕获场景中动态创建的可保存对象（如运行时生成的掉落物品等）。

**空值防御**：`LoadGame()`中对`GameData`的每个子字段进行空值检查，若反序列化后某字段为null（如旧版本存档缺少新增字段），则初始化为空集合，保证向后兼容。

#### 4.1.2 FileDataHandler——文件I/O实现

`FileDataHandler`封装了底层的文件读写操作，不依赖Unity的MonoBehaviour，为纯C#类。

**写入实现**：使用`Directory.CreateDirectory()`确保目标目录存在，通过`JsonUtility.ToJson(_data, true)`将`GameData`对象序列化为带缩进格式的JSON字符串，然后使用`FileStream`（`FileMode.Create`）配合`StreamWriter`写入磁盘。采用`using`语句确保流资源正确释放。

**读取实现**：先通过`File.Exists()`检查文件存在性，再使用`FileStream`（`FileMode.Open`）配合`StreamReader`读取全部文本内容，最终通过`JsonUtility.FromJson<GameData>()`反序列化为`GameData`对象。全程由try-catch包裹，捕获IO异常并通过`Debug.LogError`输出错误信息。

**加密机制**：预留了基于XOR的简单加密方案。`EncryptDecrypt()`方法将待加密字符串的每个字符与密钥`"alexdev"`对应位置的字符进行异或运算，加密和解密使用同一方法（XOR的自反性）。当前加密逻辑被注释，`encryptData`字段默认为`false`，可根据需要启用。

#### 4.1.3 SerializableDictionary——序列化兼容实现

`SerializableDictionary<TKey, TValue>`是解决Unity `JsonUtility`不支持`Dictionary`序列化的关键封装。该类继承自`Dictionary<TKey, TValue>`并实现了`ISerializationCallbackReceiver`接口。

- **OnBeforeSerialize()**：序列化前，遍历字典将键和值分别存入`List<TKey> keys`和`List<TValue> values`两个Unity可序列化的列表字段。
- **OnAfterDeserialize()**：反序列化后，清空字典，然后遍历并行的键值列表逐条`Add`重建字典。同时进行键值数量一致性校验。

该泛型类实现了类型安全的字典序列化，被`GameData`中的`skillTree`、`inventory`、`checkpoints`和`volumeSettings`四个字段复用，避免了为每种字典类型编写重复的序列化逻辑。

### 4.2 子系统接口实现分析

#### 4.2.1 GameManager——关卡与死亡数据处理

GameManager的`SaveData()`保存三项数据：调用`FindClosestCheckpoint()`遍历所有检查点找到距离玩家最近且已激活的检查点作为重生点；遍历所有检查点保存其激活状态键值对；保存当前损失货币的数量及玩家位置坐标。

特别值得关注的是`LoadData()`使用协程延迟0.1秒执行，原因在于场景加载后检查点对象和玩家对象的`Start()`方法执行时序不确定，延迟等待确保所有依赖对象完成初始化后再进行位置传送和状态恢复。

#### 4.2.2 Inventory——物品数据的ID引用方案

Inventory的持久化实现体现了资源引用在序列化中的经典处理方式。由于`ItemData`为ScriptableObject资源（Unity引擎管理的资源对象），序列化时无法直接存储对象引用，因此系统采用**ID引用间接恢复**方案：

- **保存时**：遍历`inventoryDictianory`、`stashDictianory`和`equipmentDictionary`，提取每个物品的`itemId`（Unity自动生成的GUID字符串），连同堆叠数量存入`GameData`的`inventory`字典和`equipmentId`列表。
- **加载时**：遍历存档中的物品ID，在`itemDataBase`（预加载的全部物品数据列表，通过编辑器脚本`FillUpItemDataBase()`从`Assets/Data/Item`目录收集所有`ItemData` ScriptableObject）中逐条匹配，构建`loadedItems`和`loadedEquipment`临时列表。
- **应用时**：在`DelayAddStartingItems()`协程中（等待一帧确保`LoadData`已执行），若`loadedItems`非空则逐条实例化恢复，否则回退到添加`startingItems`初始物品。

#### 4.2.3 技能类——分散式状态保存

七个技能类（SwordSkill、ParrySkill、DodgeSkill、DashSkill、CrystalSkill、BlackholeSkill、CloneSkill）均实现`ISaveManager`接口，各自负责保存其子技能按钮的解锁状态。每个技能将其子技能的解锁布尔值以UI按钮对象名称为键存入`_data.skillTree`字典。加载时通过协程`DelayCheckUnlock()`延迟一帧检查解锁状态并应用对应效果（如激活连击检测、解锁瞬移能力等）。

### 4.3 关键技术点总结

| 技术点 | 实现方案 | 说明 |
|--------|----------|------|
| 序列化方案 | Unity JsonUtility | 轻量级JSON序列化，Unity内置支持 |
| Dictionary序列化 | SerializableDictionary + ISerializationCallbackReceiver | 键值列表转换桥接 |
| 资源引用持久化 | 物品GUID间接引用 + 物品数据库查找 | 解耦资源对象与存档数据 |
| 接口发现 | FindObjectsOfType + 接口类型过滤 | 运行时动态扫描，无需手动绑定 |
| 延迟加载 | 协程 yield return null / WaitForSeconds | 解决跨系统初始化时序依赖 |
| 空值防御 | 逐字段null检查 + 默认值初始化 | 存档向后兼容 |
| 文件安全 | try-catch + using语句块 | 异常隔离与资源释放保障 |
| 加密预留 | XOR异或加密（已注释） | 可选的存档防篡改方案 |
| 自动保存时机 | OnApplicationQuit + Die + Restart + ManualExit | 多触发点覆盖进度保护 |
